/** Safe to import from client components (no server-only deps). */

export const FREE_TRIP_GENERATIONS = 1;

export const FAIR_USE_DISCLAIMER =
  "Unlimited trip generations are subject to fair use so we can keep quality high and costs predictable. Normal personal travel planning is always fine; automated or abusive use may be limited.";

export const PRO_PLAN_PRICE_USD = 19;
export const PRO_PLAN_LABEL = `$${PRO_PLAN_PRICE_USD}/mo`;
